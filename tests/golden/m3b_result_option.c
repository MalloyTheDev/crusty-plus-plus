/* Generated by crustc (CRusty++ v0.1) from examples/m3b_result_option.crust */
/* Do not edit by hand. */
#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>

static void crx_panic(const char *msg) {
    fprintf(stderr, "panic: %s\n", msg);
    exit(101);
}

typedef struct { const char *ptr; size_t len; } crx_str;

typedef struct { int32_t tag; int32_t some; } crx_option_i32;  /* tag 1 = Some, 0 = None */

typedef struct { int32_t tag; union { int32_t ok; int32_t err; } payload; } crx_result_i32_i32;  /* tag 1 = Ok, 0 = Err */

typedef struct { int32_t tag; union { size_t ok; int32_t err; } payload; } crx_result_usize_i32;  /* tag 1 = Ok, 0 = Err */

static int32_t crx_unwrap_option_i32(crx_option_i32 o) {
    if (o.tag != 1) crx_panic("unwrap on None");
    return o.some;
}

static int32_t crx_unwrap_result_i32_i32(crx_result_i32_i32 r) {
    if (r.tag != 1) crx_panic("unwrap on Err");
    return r.payload.ok;
}

static size_t crx_unwrap_result_usize_i32(crx_result_usize_i32 r) {
    if (r.tag != 1) crx_panic("unwrap on Err");
    return r.payload.ok;
}

int main(void) {
    crx_result_i32_i32 ok_r = (crx_result_i32_i32){ .tag = 1, .payload.ok = 42 };
    crx_result_i32_i32 err_r = (crx_result_i32_i32){ .tag = 0, .payload.err = 7 };
    bool a = (ok_r.tag == 1);
    bool b = (err_r.tag == 0);
    int32_t v = crx_unwrap_result_i32_i32(ok_r);
    crx_option_i32 some_o = (crx_option_i32){ .tag = 1, .some = 5 };
    int32_t w = crx_unwrap_option_i32(some_o);
    crx_option_i32 maybe = (crx_option_i32){ .tag = 0 };
    maybe = (crx_option_i32){ .tag = 1, .some = 9 };
    int32_t x = crx_unwrap_option_i32(maybe);
    crx_str msg = (crx_str){ "abcd", 4 };
    crx_result_usize_i32 len_r = (crx_result_usize_i32){ .tag = 1, .payload.ok = msg.len };
    size_t n = crx_unwrap_result_usize_i32(len_r);
    int32_t result = 0;
    if (a) {
    } else {
        result = 1;
    }
    if (b) {
    } else {
        result = 1;
    }
    if ((v == 42)) {
    } else {
        result = 1;
    }
    if ((w == 5)) {
    } else {
        result = 1;
    }
    if ((x == 9)) {
    } else {
        result = 1;
    }
    if ((n == 4)) {
    } else {
        result = 1;
    }
    return result;
}

// Function-call arguments evaluate left-to-right (codegen hoists in order).
fn one() -> i32 { return 1; }
fn two() -> i32 { return 2; }
fn add2(a: i32, b: i32) -> i32 { return a + b; }
fn main() -> i32 {
    let x: i32 = add2(one(), two());
    return x - 3;            // 1 + 2 - 3 == 0
}

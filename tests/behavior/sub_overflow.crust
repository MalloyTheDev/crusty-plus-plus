// Behavior test: i32 subtraction overflow must abort with exit code 101.
fn main() -> i32 {
    let m: i32 = -2147483647 - 1;   // i32::MIN, computed without overflow
    let r: i32 = m - 1;             // checked subtraction overflows -> panic
    return r;
}

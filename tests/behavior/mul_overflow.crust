// Behavior test: i32 multiplication overflow must abort with exit code 101.
fn main() -> i32 {
    let m: i32 = 2147483647;   // i32::MAX
    let r: i32 = m * 2;        // checked multiplication overflows -> panic
    return r;
}
